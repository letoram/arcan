--
-- Target window
-- Builds upon an established media window in frameserver mode
-- Adds options based on the capabilities of the target session in question
-- and for mapping input along with logic for netplay / netsynch
-- (assumes bar in 'tt' slot)
--
function awbwnd_target(pwin)
		
end

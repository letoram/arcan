#ifndef HAVE_ALT_OPAQUE
#define HAVE_ALT_OPAQUE

/* forward declarations used as opaque types, just avoid pulling in the full
 * definitions here as they are not really needed. */
struct arcan_vobject;
typedef struct arcan_vobject arcan_vobject;
struct arcan_frameserver;
typedef struct arcan_event arcan_event;
enum ARCAN_SEGID;

#endif
